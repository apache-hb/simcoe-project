
src = files([
    'src/main.cpp'
])

deps = [
    core, system, logs, threads, imgui, render, input, archive, config,
    cthulhu.get_variable('backtrace'),
    cthulhu.get_variable('format_core'),
    cthulhu.get_variable('io'),
    tracy
]

pch = files('src/stdafx.hpp')
