#include "ui/ui.hpp"

#include "core/macros.h"

using namespace sm;
using namespace sm::ui;

void Canvas::layout(CanvasDrawData& data, const IWidget& widget) {
    CT_UNUSED(data);
    CT_UNUSED(widget);
}
